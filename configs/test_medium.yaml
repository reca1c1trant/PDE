# Test Configuration for V2 Model

# Checkpoint to evaluate
checkpoint: "./checkpoints_e2e_medium/best.pt"

# Model config (must match training)
model_config: "./configs/e2e_medium.yaml"

# Dataset (seed and train_ratio must match training for correct val split)
dataset:
  path: "/home/msai/song0304/code/PDE/data"
  train_ratio: 0.9
  seed: 42  # Must match training seed for consistent val split

# Test parameters
test:
  Lx: 2.0
  Ly: 2.0
  plot: false              # 默认false，不画图
  channel_plot: 0          # 画哪个channel
  model_name: "pde_e2e_v2"
  input_steps: 1          # 用几个timestep作为输入
  batch_size: 8           # 每个样本生成的随机时间切片数
  num_samples: 100        # 评估多少个样本 (null = 全部val集)
  seed: 57                # 随机采样timestep的种子


