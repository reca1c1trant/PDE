# Test Configuration

# Model checkpoint
checkpoint: "./checkpoints_1b/best.pt"

# Model config (用于加载模型结构)
model_config: "./configs/llama_1b.yaml"

# Test dataset
dataset:
  path: "/home/msai/song0304/code/PDE/data"

# Test parameters
test:
  input_steps: 4          # 用几个timestep作为输入
  batch_size: 4           # 每次评估的batch大小
  num_samples: 100       # 评估多少个样本 (null = 全部)
  seed: 42                # 随机种子 (保证可复现)
