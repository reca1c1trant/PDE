# Test Configuration

# Model checkpoint
checkpoint: "./checkpoints_1b/best.pt"

# Model config (用于加载模型结构)
model_config: "./configs/llama_1b.yaml"

# Test dataset
dataset:
  path: "/home/msai/song0304/code/PDE/data"

# Test parameters
test:
  input_steps: 4          # 用几个timestep作为输入
  slices_per_sample: 4    # 每个样本取几个不同的时间切片
  batch_size: 4           # DataLoader batch大小
  num_samples: 100        # 评估多少个基础样本 (null = 全部)
  seed: 42                # 随机种子 (保证可复现)
  nrmse_sigma: [0.143810, 0.111739, 0.0, 0.0, 0.0, 0.0]  # 训练集全局sigma
