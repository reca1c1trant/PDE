# Test configuration for FFT V3 model
# Usage: python test_fft.py --config configs/test_v3.yaml

# Model config (training config)
model_config: "configs/e2e_v3.yaml"

# Checkpoint path
checkpoint: "./checkpoints_fft_v3/best.pt"

# Dataset
dataset:
  path: "/home/msai/song0304/code/PDE/data"
  train_ratio: 0.9

# Test settings
seed: 42
batch_size: 4
num_samples: null  # null = use all val samples

# nRMSE sigma (same as training, set to null for RMSE only)
nrmse_sigma: [0.143810, 0.111739, 0.0, 0.0, 0.0, 0.0]
